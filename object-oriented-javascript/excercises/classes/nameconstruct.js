console.log("Hello".constructor.name);
console.log([1,2,3].constructor.name);
console.log({name: 'Srdjan'}.constructor.name);

//use the constructor.name method to find the name of the value's constructor
//change the code so that it names the constructor of the types